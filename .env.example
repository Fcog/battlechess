# PostgreSQL connection string (local or hosted, e.g. Neon, Railway, Supabase).
# Docker (default user/password):  postgresql://postgres:postgres@localhost:5432/chess_app
# Example Neon:   postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/chess_app"

# NextAuth (required for auth)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Optional: Google OAuth (set NEXT_PUBLIC_GOOGLE_ENABLED=true to show Google sign-in)
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""
# NEXT_PUBLIC_GOOGLE_ENABLED="false"
